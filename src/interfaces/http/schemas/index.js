const priceConfiguration = require('interfaces/http/schemas/price-configuration');
const pricingModel = require('interfaces/http/schemas/pricing-model');

module.exports = {
	priceConfiguration,
	pricingModel
};
