const errorHandlerMiddleware = require('interfaces/http/middlewares/error-handler-middleware');
const responseHandlerMiddleware = require('interfaces/http/middlewares/response-handler-middleware');

module.exports = {
	errorHandlerMiddleware,
	responseHandlerMiddleware
};
